server:
  port: 7709

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/user_db
    username: root
    password: Vignesh@2808

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
eureka:
  client:
    service-url:
      defaultZone: http://localhost:7777/eureka/ 
management:
  endpoints:
    web:
      exposure:
        include: "*"
