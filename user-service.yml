server:
  port: 7708

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/user_db
    username: root
    password: Vignesh@2808

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

management:
  endpoints:
    web:
      exposure:
        include: "*"
